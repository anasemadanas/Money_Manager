services:
  - type: web
    name: money-manager
    env: docker
    plan: free

    dockerfilePath: Dockerfile

    healthCheckPath: /actuator/health

    envVars:
      - key: SPRING_PROFILES_ACTIVE
        value: prod

      - key: PORT
        value: 8080