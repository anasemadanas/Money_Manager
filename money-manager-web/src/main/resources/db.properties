# Local defaults only. Hosting should set DATABASE_URL, DATABASE_USERNAME, and DATABASE_PASSWORD.
# Supabase style URLs are also accepted, for example:
# DATABASE_URL=postgres://user:pass@host:5432/dbname
# The app will convert them to JDBC form automatically.
db.url=jdbc:postgresql://localhost:5432/moneymanager
db.username=postgres
db.password=123456
